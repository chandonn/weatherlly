import { useContext, useEffect } from "react"
import { getGeolocationSearchResults } from "../services"
import { Store } from "../store/state"

export const withSearchResults = (query: string) => {
  
}
